!非同期モード
// ネットワーク通信を利用して
// 犬の画像をランダムで表示する
// Dog API  https://dog.ceo/dog-api/

// データ(JSON形式)を受信する
API＝「https://dog.ceo/api/breeds/image/random」。
APIからAJAX受信。

// 受信したデータをSという辞書型配列へ入れる
対象をJSONデコードしてSに代入。

// Sの内容を表示させる
Sを反復
　　「{対象キー}:{対象}」を表示。
ここまで。

// 犬の画像を表示させる。
S@["message"]を絵追加。

