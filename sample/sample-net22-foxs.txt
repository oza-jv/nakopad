!非同期モード
// ネットワーク通信を利用して
// キツネの画像をランダムで表示する
// randomfox.ca 

// データ(JSON形式)を受信する
API＝「https://randomfox.ca/floof/」。
APIからAJAX受信。

// 受信したデータを利用しやすい形式に解析する
対象をJSONデコードして利用データに代入。

// 解析した内容を表示させる
利用データを反復
　　「{対象キー}:{対象}」を表示。
ここまで。

// キツネの画像を表示させる。
利用データ["image"]を絵追加。
