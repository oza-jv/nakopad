!非同期モード
// ネットワーク通信を利用して
// キツネの画像をランダムで表示する
// randomfox.ca 

// データ(JSON形式)を受信する
API＝「https://randomfox.ca/floof/」。
APIからAJAX受信。

// 受信したデータをSという辞書型配列へ入れる
対象をJSONデコードしてSに代入。

// Sの内容を表示させる
Sを反復
　　「{対象キー}:{対象}」を表示。
ここまで。

// キツネの画像を表示させる。
S@["image"]を絵追加。

